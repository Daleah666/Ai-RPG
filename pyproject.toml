[project]
name = "leader-hq"
version = "0.1.0"
description = "Nova / code_leader Google Drive bot bus"
requires-python = ">=3.11"
dependencies = []

[project.scripts]
code-leader = "leader_hq.cli:main"

[tool.setuptools.packages.find]
include = ["leader_hq*"]
